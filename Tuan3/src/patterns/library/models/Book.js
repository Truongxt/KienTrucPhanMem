class Book {
  constructor({ id, title, author, genre, type }) {
    this.id = id;
    this.title = title;
    this.author = author;
    this.genre = genre;
    this.type = type;
  }
}

module.exports = Book;
