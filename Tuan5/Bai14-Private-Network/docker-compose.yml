version: '3.8'

services:
  service1:
    image: alpine
    command: sleep infinity
    networks:
      - private_net

  service2:
    image: alpine
    command: sleep infinity
    networks:
      - private_net

networks:
  private_net:
    driver: bridge
    internal: true
